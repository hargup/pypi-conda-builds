package:
  name: splunk-sdk
  version: "1.3.1"

source:
  fn: splunk-sdk-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/splunk-sdk/splunk-sdk-1.3.1.tar.gz
  md5: fe315bbf60d8eeba4574b21adf2c3678
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - splunk-sdk = splunk-sdk:main
    #
    # Would create an entry point called splunk-sdk that calls splunk-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - splunklib
    - splunklib.modularinput
    - splunklib.searchcommands
    - splunklib.searchcommands.splunk_csv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/splunk/splunk-sdk-python
  license: Apache Software License
  summary: 'The Splunk Software Development Kit for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
