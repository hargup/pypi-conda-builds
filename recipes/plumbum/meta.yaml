package:
  name: plumbum
  version: "1.4.2"

source:
  fn: plumbum-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plumbum/plumbum-1.4.2.tar.gz
  md5: 38b526af9012a5282ae91dfe372cefd3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plumbum = plumbum:main
    #
    # Would create an entry point called plumbum that calls plumbum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - plumbum
    - plumbum.cli
    - plumbum.commands
    - plumbum.fs
    - plumbum.machines
    - plumbum.path

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plumbum.readthedocs.org
  license: MIT License
  summary: 'Plumbum: shell combinators library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
