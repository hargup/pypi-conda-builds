package:
  name: python-cloudfiles
  version: "1.7.11"

source:
  fn: python-cloudfiles-1.7.11.tar.gz
  url: https://pypi.python.org/packages/source/p/python-cloudfiles/python-cloudfiles-1.7.11.tar.gz
  md5: 34aa2678078a884dc6752c60d4c6bb90
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-cloudfiles = python-cloudfiles:main
    #
    # Would create an entry point called python-cloudfiles that calls python-cloudfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudfiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rackspace/python-cloudfiles
  license: MIT License
  summary: 'CloudFiles client library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
