package:
  name: zake
  version: "0.2.2"

source:
  fn: zake-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zake/zake-0.2.2.tar.gz
  md5: 076dcb58f5cb8b50f575fc0ca7e810b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zake = zake:main
    #
    # Would create an entry point called zake that calls zake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kazoo >=1.3.1,!=2.1
    - six

  run:
    - python
    - kazoo >=1.3.1,!=2.1
    - six

test:
  # Python imports
  imports:
    - zake
    - zake.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yahoo/Zake
  license: Apache Software License
  summary: 'A python package that works to provide a nice set of testing utilities for the kazoo library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
