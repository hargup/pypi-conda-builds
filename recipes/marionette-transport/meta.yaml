package:
  name: marionette-transport
  version: "0.5"

source:
  fn: marionette-transport-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/marionette-transport/marionette-transport-0.5.tar.gz
  md5: 91c2baa50484445974cc24e07e62cced
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marionette-transport = marionette-transport:main
    #
    # Would create an entry point called marionette-transport that calls marionette-transport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marionette_transport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://developer.mozilla.org/en-US/docs/Marionette
  license: MPL
  summary: 'Transport layer for Marionette client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
