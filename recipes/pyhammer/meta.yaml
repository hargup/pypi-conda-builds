package:
  name: pyhammer
  version: "0.5.26"

source:
  fn: pyhammer-0.5.26.zip
  url: https://pypi.python.org/packages/source/p/pyhammer/pyhammer-0.5.26.zip
  md5: 82dc1d5d56db42bd989100209d6fae54
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyhammer = pyhammer:main
    #
    # Would create an entry point called pyhammer that calls pyhammer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyhammer
    - pyhammer.filters
    - pyhammer.reporters
    - pyhammer.tasks
    - pyhammer.tasks.git
    - pyhammer.tasks.helpers
    - pyhammer.tasks.io
    - pyhammer.tasks.svn
    - pyhammer.tasks.text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/webbers/pyhammer
  license: MIT License
  summary: 'Build automation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
