package:
  name: django-redis
  version: "4.1.0"

source:
  fn: django-redis-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis/django-redis-4.1.0.tar.gz
  md5: 2a00e866cb1ed3bd9bd90642abc41d92
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis = django-redis:main
    #
    # Would create an entry point called django-redis that calls django-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.10.0
    - msgpack-python >=0.4.6

  run:
    - python
    - redis >=2.10.0
    - msgpack-python >=0.4.6

test:
  # Python imports
  imports:
    - django_redis
    - django_redis.client
    - django_redis.serializers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-redis
  license: BSD License
  summary: 'Full featured redis cache backend for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
