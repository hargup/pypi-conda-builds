package:
  name: simplekml
  version: "1.2.8"

source:
  fn: simplekml-1.2.8.zip
  url: https://pypi.python.org/packages/source/s/simplekml/simplekml-1.2.8.zip
  md5: 16ac3ad2639c7e50ec769baf33a0278e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplekml = simplekml:main
    #
    # Would create an entry point called simplekml that calls simplekml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplekml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/simplekml/
  license: GNU General Public License (GPL)
  summary: 'A Simple KML creator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
