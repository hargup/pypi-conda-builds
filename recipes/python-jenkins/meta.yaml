package:
  name: python-jenkins
  version: "0.4.7"

source:
  fn: python-jenkins-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-jenkins/python-jenkins-0.4.7.tar.gz
  md5: 8f16638c8b0aa3a0d6343bdce854ca6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-jenkins = python-jenkins:main
    #
    # Would create an entry point called python-jenkins that calls python-jenkins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Python bindings for the remote Jenkins API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
