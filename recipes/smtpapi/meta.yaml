package:
  name: smtpapi
  version: "0.2.0"

source:
  fn: smtpapi-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/smtpapi/smtpapi-0.2.0.tar.gz
  md5: 9c853f663023c6335b31817448ad5d9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smtpapi = smtpapi:main
    #
    # Would create an entry point called smtpapi that calls smtpapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smtpapi
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Simple wrapper to use SendGrid SMTP API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
